export interface UiStructure {
  isLoading: boolean;
  showModal: boolean;
  isError: boolean;
  modalMessage: string;
}

export interface ModalStructure {
  showModal: boolean;
  isError: boolean;
  modalMessage: string;
}

export default UiStructure;
